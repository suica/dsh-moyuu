{
  "name": "dsh-moyuu-session-write-sync",
  "version": "0.1.0",
  "description": "Feature: session write-sync consumer for the cross-process write-lock backend — recognizes the locking backend's SESSION_ADVANCED (another process advanced the log) and SESSION_LOCK_TIMEOUT (transient contention) signals at the physical-write boundary, and surfaces a structured session/sync-conflict event instead of silently corrupting or dropping writes.",
  "keywords": [
    "dsh",
    "deepseek-harness",
    "plugin",
    "session",
    "persistence",
    "sync",
    "conflict"
  ],
  "type": "module",
  "main": "index.js",
  "exports": {
    ".": "./index.js",
    "./cordis.patch.yml": "./cordis.patch.yml",
    "./package.json": "./package.json"
  },
  "dependencies": {
    "dsh-moyuu-session-write-lock": "workspace:*"
  },
  "peerDependencies": {
    "@deepseek-ai/dsh-session-persistence-jsonl": "*",
    "@deepseek-ai/dsh-session": "*"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "license": "MIT"
}
